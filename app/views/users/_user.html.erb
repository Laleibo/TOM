
  <p class="dis_title"><%= @user.first_name%>'s Preferences</p>
  <table class="responsive-table bordered">
    <tbody>
      <tr>
        <td class="header"><strong>First Name:</strong></td>
        <td class="info"><%= @user.first_name %></td>
      </tr>
      <tr>
        <td><strong>Last Name:</strong></td>
        <td class="info"><%= @user.last_name %></td>
      </tr>
      <tr>
        <td><strong>Birthday:</strong></td>
        <td class="info"><%= User.birthday_day(@user.birthday) %></td>
      </tr>
      <tr>
        <td><strong>My Products </strong></td>
        <td><%= @user.variety %></td>
      </tr>
      <tr>
        <td><strong>My Flow is </strong></td>
        <td><%= @user.flow %></td>
      </tr>
      <tr>
        <td><strong>I like my products to be </strong></td>
        <td><%= @user.scent %></td>
      </tr>
    </tbody>
  </table>
  <br><br>

  <%= link_to "Edit User", edit_profile_user_path(@profile.id, @user.id), id: "user_edit" %>
  <%#= button_to "Remove User", profile_user_path(params[:profile_id], @user), method: "delete" %>
<br><br><br>
